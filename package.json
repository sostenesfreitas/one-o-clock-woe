{
  "name": "woe-party",
  "version": "1.0.0",
  "private": true,
  "description": "WoE party organizer — single-file app. This package.json exists ONLY to run the dependency-free test suite; the app itself has no build step and no runtime dependencies.",
  "scripts": {
    "test": "node test/run.js",
    "parse-check": "node test/parse-check.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
