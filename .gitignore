# CodeGraph local index — tooling, rebuilt per machine, not for the repo
.codegraph/

# npm — package.json exists only to run the zero-dependency test suite
node_modules/
package-lock.json

# WhatsApp bot — session credentials + browser cache (never commit)
bot/.wwebjs_auth/
bot/.wwebjs_cache/

# Local RTDB backups exported before destructive ops (see RUNBOOK.md) — keep off-repo
backups/

# OS / editor cruft
.DS_Store
Thumbs.db
*.bak-*

# Scratch / throwaway helper files used during a session (never commit these)
_*
